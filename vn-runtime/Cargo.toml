[package]
name = "vn-runtime"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0.18"

[dev-dependencies]
insta = { version = "1", features = ["yaml"] }

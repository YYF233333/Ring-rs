[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"

[alias]
pack = "run -p asset-packer --"

# -------- Local quality gates (single-developer workflow) --------
# Fast, composable building blocks:
fmt-check = "fmt --all -- --check"
fmt-all = "fmt --all"
clippy-all = "clippy --workspace --all-targets"
clippy-deny = "clippy --workspace --all-targets -- -D warnings"
test-all = "test --workspace"
test-runtime = "test -p vn-runtime"
test-host-lib = "test -p host --lib"
test-dioxus = "test -p host-dioxus --lib"

# One-command local gate (cross-platform).
# Runs: fmt -> clippy（自动 fix）-> test
check-all = "run -p xtask -- check-all"

# -------- Coverage (local) --------
# Requires: cargo install cargo-llvm-cov && rustup component add llvm-tools-preview
cov = "run -p xtask -- cov"

# -------- Script checking --------
# Static analysis for VN scripts: syntax, labels, resource references.
script-check = "run -p xtask -- script-check"

# -------- Mutation testing --------
# Requires: cargo install cargo-mutants
# Default target is vn-runtime. Use --package host or cargo mutants-host-check for host.
# Pass extra args after --: cargo mutants-check -- --file vn-runtime/src/state.rs
mutants-check = "run -p xtask -- mutants"
mutants-host-check = "run -p xtask -- mutants --package host"

# -------- Symbol index generation --------
gen-symbols = "run -p xtask -- gen-symbols"
[workspace]
resolver = "3"

members = [
    "vn-runtime",
    "host",
    "tools/asset-packer",
    "tools/xtask",
    "tools/rendering-poc",
]

default-members = ["host"]

[profile.dev.package."*"]
opt-level = 1

[profile.release]
debug = true
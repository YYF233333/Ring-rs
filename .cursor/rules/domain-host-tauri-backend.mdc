---
description: "Domain: Tauri 后端 — Rust IPC commands, state management, command executor"
globs: host-tauri/src-tauri/**
alwaysApply: false
---

# Tauri 后端（host-tauri/src-tauri）

## 摘要导航

- [host-tauri 模块总览](docs/engine/architecture/module-summaries/host-tauri.md)
- [state](docs/engine/architecture/module-summaries/host-tauri/state.md)
- [commands](docs/engine/architecture/module-summaries/host-tauri/commands.md)
- [command_executor](docs/engine/architecture/module-summaries/host-tauri/command-executor.md)
- [render_state](docs/engine/architecture/module-summaries/host-tauri/render-state.md)
- [audio](docs/engine/architecture/module-summaries/host-tauri/audio.md)
- [resources](docs/engine/architecture/module-summaries/host-tauri/resources.md)

## 关键不变量

- `commands.rs` 中的 IPC 命令必须是**薄代理**：lock → 调用 AppStateInner 方法 → 返回结果。业务逻辑在 `state.rs`。
- 新增 IPC 命令时必须同步：①`commands.rs` 添加 `#[command]` 函数 ②`lib.rs` 注册到 `invoke_handler` ③`debug_server.rs` 添加 dispatch 分支。
- `RenderState` 的 Rust 定义与 `host-tauri/src/types/render-state.ts` 的 TypeScript 定义必须保持同步（字段名、类型映射）。
- `AppState.inner` 是 `Arc<Mutex<AppStateInner>>`，被 Tauri 和 debug HTTP server 共享。锁范围必须最小化。

## Do / Don't

- **Do** 在 `state.rs` 中实现业务逻辑方法，让 `commands.rs` 保持薄代理。
- **Do** 新增 Command 处理分支时同步更新 `render_state.rs` 中的字段。
- **Do** 使用 `tracing::info/warn/error` 记录关键事件，不要用 `println!`。
- **Don't** 在 `commands.rs` 中写业务逻辑——下沉到 `state.rs` 的方法。
- **Don't** 在 `command_executor.rs` 中直接操作音频——通过 `AudioCommand` 中转，由 `state.rs` 处理。
- **Don't** 跨 await point 持有 `Mutex` 锁（debug_server 中使用 std::sync::Mutex）。

---
description: "Domain: Tauri 前端 — Vue 3 + TypeScript, VN rendering, UI screens"
globs: host-tauri/src/**/*.vue,host-tauri/src/**/*.ts
alwaysApply: false
---

# Tauri 前端（host-tauri/src）

## 摘要导航

- [前端总览](docs/engine/architecture/module-summaries/host-tauri/frontend.md)

## 关键不变量

- 所有引擎/游戏相关的后端调用必须通过 `useEngine()` (`composables/useEngine.ts`)。`useEngine` 内部使用 `callBackend()`；Vue 组件和 `App.vue` **不得**直接 import `callBackend`。
- 底层传输抽象 `callBackend()` (`composables/useBackend.ts`) 仅限 composable 内部使用，不得直接 `import { invoke } from "@tauri-apps/api/core"`。唯一例外是 `useBackend.ts` 自身。
- 所有日志必须通过 `createLogger()` (`composables/useLogger.ts`)，不得直接 `console.log/debug/error`。
- `types/render-state.ts` 的 TypeScript 类型定义必须与 Rust `render_state.rs` 保持同步。字段名使用 snake_case（与 Rust serde 序列化一致）。
- 资源 URL 必须通过 `resolveAssetSrc()` 或 `useAssets().assetUrl()` 获取，不得硬编码路径。
- 前端只负责**渲染**，不持有游戏逻辑状态——所有状态由 Rust 后端管理，前端通过 tick 轮询获取最新 RenderState。
- VN 渲染组件（`vn/`）和系统 UI 组件（`screens/`）使用纯 emit 模式与父组件通信，**不得**直接调用后端。

## Do / Don't

- **Do** 使用 Composition API (`<script setup>`) 和 TypeScript。
- **Do** 将可复用的状态逻辑封装为 composable（`composables/useXxx.ts`）。
- **Do** VN 渲染组件放 `vn/`，系统 UI 放 `screens/`，通用 UI 放 `components/`。
- **Do** 组件通过 emit 向上通信，由 `App.vue`（通过 `useEngine`）统一处理后端调用。
- **Don't** 在 Vue 组件（包括 `App.vue`）中直接 `import { callBackend }` — 用 `useEngine` 封装。
- **Don't** 在 Vue 组件中直接 `console.log` — 用 `createLogger`。
- **Don't** 在前端维护游戏状态（如对话进度、角色位置）— 这是后端职责。

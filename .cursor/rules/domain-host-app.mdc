---
description: "Domain: 应用编排层 — app lifecycle, update loop, command executor, command handlers"
globs: host/src/app/**,host/src/command_executor/**,host/src/host_app.rs,host/src/egui_actions.rs
alwaysApply: false
---

# 应用编排层（host-app）

## 摘要导航

- [app](docs/engine/architecture/module-summaries/host/app.md)
- [app_update](docs/engine/architecture/module-summaries/host/app-update.md)
- [app_command_handlers](docs/engine/architecture/module-summaries/host/app-command-handlers.md)
- [command_executor](docs/engine/architecture/module-summaries/host/command-executor.md)
- [host_app](docs/engine/architecture/module-summaries/host/host-app.md)
- [egui_actions](docs/engine/architecture/module-summaries/host/egui-actions.md)

## 关键不变量

- `CommandExecutor` 负责"Command → 状态变更 + 输出事件"，`command_handlers` 负责"消费输出事件 → 驱动外部系统"。不要混淆两层职责。
- `AppMode` 状态机控制 update 分发。新增模式须在 `modes.rs` 添加分支并处理进入/退出逻辑。
- `PlaybackMode::{Normal,Auto,Skip}` 控制推进速度。Skip 模式须走统一入口 `skip_all_active_effects()`。
- 所有 Runtime Command 须在 executor 中有处理分支；遗漏会导致静默丢弃。

## Do / Don't

- **Do** 在 executor 新增 Command 分支时同步更新 `types.rs` 的输出事件枚举。
- **Do** 保持 `update/mod.rs` 作为薄编排层，业务逻辑下沉到子模块。
- **Don't** 在 `command_executor` 中直接操作音频/GPU——通过输出事件让 `command_handlers` 处理。
- **Don't** 在 update 循环中硬编码帧率假设；使用 `dt` 参数。
